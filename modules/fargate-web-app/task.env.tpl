# empty task.env file for seeding s3 configs bucket - file must exist for ECS task to launch, even if empty
# note: quotes are not stripped!
# myvar="foo" will provide the literal string `"foo"`, not `foo`